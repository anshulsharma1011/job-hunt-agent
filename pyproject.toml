[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "job-hunt-agent"
version = "0.1.0"
requires-python = ">=3.12"

[project.scripts]
job-hunt = "cli.main:cli"

[tool.setuptools.packages.find]
where = ["."]
